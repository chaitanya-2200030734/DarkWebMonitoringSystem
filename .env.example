# Server Configuration
PORT=3000
NODE_ENV=production

# Debug Logging (set to 'true' to enable verbose logs)
ENABLE_DEBUG_LOGS=false

# CORS Configuration (comma-separated list of allowed origins)
# For production, set this to your frontend domain(s)
# Example: ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
ALLOWED_ORIGINS=*

# Google Safe Browsing API Key (optional, for enhanced threat detection)
GOOGLE_SAFE_BROWSING_API_KEY=

# Frontend API URL (optional, defaults to relative path in production)
# Only set this if your frontend and backend are on different domains
# VITE_API_URL=https://api.yourdomain.com

# Tor Configuration (automatically detected, no need to set)
# TOR_PROXY_PORT_1=9050
# TOR_PROXY_PORT_2=9150
